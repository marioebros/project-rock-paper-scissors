function playRound(playerSelection, computerSelection) {
    function computerPlay(0, 2) {
        0 = Rock; 
        1 = Paper;
        2 = Scissors;
        return Math.floor(Math.random(3));
    }

    computerPlay();
    // your code here!
  
return (playRound(playerSelection, computerSelection));
}